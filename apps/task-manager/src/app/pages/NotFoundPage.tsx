import { Typography } from "@task-manager/components";

const NotFoundPage = () => {
  return (
    <Typography as="h2">
      Uh oh... seems there is nothing to see!
    </Typography>
  );
};

export default NotFoundPage;
